import AutocompletePerson from './AutocompletePerson'
import { shallow } from 'enzyme'
import React from 'react'

it('does something', () => {
  const wrapper = shallow(<AutocompletePerson />)
  // expect(wrapper.find('element')).toBeTruthy()
})

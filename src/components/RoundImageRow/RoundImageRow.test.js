import RoundImageRow from './RoundImageRow'
import { shallow } from 'enzyme'
import React from 'react'

it('does something', () => {
  const wrapper = shallow(<RoundImageRow />)
  // expect(wrapper.find('element')).toBeTruthy()
})

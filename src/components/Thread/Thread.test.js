import Thread from './Thread'
import { shallow } from 'enzyme'
import React from 'react'

it('does something', () => {
  const wrapper = shallow(<Thread />)
  // expect(wrapper.find('element')).toBeTruthy()
})

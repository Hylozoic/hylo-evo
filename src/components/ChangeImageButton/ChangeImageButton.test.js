import ChangeImageButton from './ChangeImageButton'
import { shallow } from 'enzyme'
import React from 'react'

it('does something', () => {
  const wrapper = shallow(<ChangeImageButton />)
  // expect(wrapper.find('element')).toBeTruthy()
})

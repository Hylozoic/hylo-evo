
// import { graphql } from 'msw'

// Add any reused/reusable handlers here for selective import into tests into this file

// Handlers in this object will be loaded by default
export const defaultHandlers = {}

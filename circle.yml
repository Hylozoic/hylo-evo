general:
  branches:
    only:
      - set-up-ci
dependencies:
  pre:
    - yarn add babel-jest
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: 7
test:
  override:
    - yarn test

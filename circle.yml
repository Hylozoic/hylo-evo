dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - yarn test
machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: 8.9.2
deployment:
  production:
    branch: master
    owner: Hylozoic
    heroku:
      appname: hylo-evo
general:
  artifacts:
    - "coverage"

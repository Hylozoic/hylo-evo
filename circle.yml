machine:
  node:
    version: 7.7.3
general:
  branches:
    only:
      - set-up-ci
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
test:
  override:
    - yarn test

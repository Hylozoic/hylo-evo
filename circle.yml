machine:
  node:
    version: 6.1.0
general:
  branches:
    only:
      - set-up-ci
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
test:
  override:
    - yarn test

general:
  branches:
    only:
      - set-up-ci
dependencies:
  override:
    - nvm install 7
    - nvm alias default 7
    - yarn
  cache_directories:
    - ~/.cache/yarn
machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
test:
  override:
    - yarn test
